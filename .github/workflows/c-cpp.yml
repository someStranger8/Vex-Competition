name: C/C++ CI

on:
  push:
    branches: [ "stable", "release" ]
  pull_request:
    branches: [ "stable", "release" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: make
      run: make
